{
  "server": {
    "hostname": "{PLT_MAIN_HOST}",
    "port": "{PLT_MAIN_PORT}",
    "logger": { "level": "{PLT_SERVER_LOGGER_LEVEL}" }
  },
  "core": {
    "connectionString": "{PLT_DB_TYPE}://{PLT_DB_USERNAME}:{PLT_DB_PASSWORD}@{PLT_DB_HOST}/{PLT_DB_NAME}"
  }
}
